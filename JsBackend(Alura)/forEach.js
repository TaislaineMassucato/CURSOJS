const array1 = ['a', 'b', 'c'];
    array1.forEach((element) => console.log(element));

const notas = [5.5, 6.5, 10, 9.5];
notas.forEach((nota) => console.log(nota));

let soma = 0;
notas.forEach((numero) => numero + 1);
   console.log(notas)

   //letra maiuscula
   let names = ['João', 'Maria', 'carlos', 'Ana'];

names.forEach((name) => name = name.toUpperCase()
);

console.log(names); // Saída: ['JOÃO', 'MARIA', 'CARLOS', 'ANA']

   
